description: Paginated response of sources
allOf:
  - $ref: ./PaginatedResponse.yaml
  - type: object
    properties:
      models:
        type: array
        items:
          $ref: ./Source.yaml
        readOnly: true

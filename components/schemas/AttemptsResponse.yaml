description: Paginated response of attempts
allOf:
  - $ref: ./PaginatedResponse.yaml
  - type: object
    properties:
      models:
        type: array
        items:
          $ref: ./Attempt.yaml
        readOnly: true

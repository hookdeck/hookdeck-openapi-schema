description: Paginated response of bulk retries
allOf:
  - $ref: ./PaginatedResponse.yaml
  - type: object
    properties:
      models:
        type: array
        items:
          $ref: ./BulkRetry.yaml
        readOnly: true
